/**
 * site.config.ts — الحقائق التي يقوم عليها الموقع
 *
 * كل ما قد يتغيّر يوماً موجود هنا، لا داخل المكوّنات. ولا نصّ عربي هنا: النصوص
 * كلها في `scripts/copy.ts`.
 */

/** لمن هذا الموقع. */
export const HER = 'رغد';

/** ومن مين. */
export const FROM = 'إبراهيم';

/* =========================================================================
   التواريخ
   =========================================================================
   كلها بساعة الحائط في دمشق، لا بـUTC. الصيغة `YYYY-MM-DD` أو
   `YYYY-MM-DDTHH:MM`. أي قيمة غير صالحة — أو `null` — تُخفي ما يعتمد عليها
   بدل أن تعرض أرقاماً مخترعة.
   ========================================================================= */

/** يوم الصدفة — أول نقطة في القصة. */
export const COINCIDENCE: string | null = '2026-01-25';

/** يوم أول حديث. */
export const FIRST_TALK: string | null = '2026-03-21';

/**
 * عيد ميلاد رغد — 6 أيلول.
 *
 * لا يظهر في HTML قبل مشهد الكشف؛ يُحقن بالـJS عند الوصول إليه.
 *
 * لكن بصراحة: في موقع ثابت تنزل الحزمة كاملة عند أول تحميل، فالتاريخ موجود
 * فيها لمن يفتح أدوات المطوّر. وهو أصلاً عيد ميلادها هي — التاريخ ليس السرّ،
 * السرّ ما بُني حوله. إخفاؤه هنا إخراجٌ لا أمان، ومن الأنزه أن يُقال ذلك.
 */
export const BIRTHDAY: string | null = '2026-09-06';

/** موعد رسالة اللطافة اليومية — بتوقيت دمشق. */
export const SWEET_HOUR = 11;
export const SWEET_MINUTE = 0;

/* =========================================================================
   القصة
   ========================================================================= */

export const STORY = {
  /**
   * القصة تبدأ من أولها في كل زيارة.
   *
   * الاستئناف أرحم عملياً، لكن هذه حكاية لا لوحة تحكّم: فتحها يعني مشاهدتها،
   * لا متابعة من حيث توقّفت.
   */
  resume: false,

  /**
   * الأغنية تبدأ عند فتح الظرف الأول، لا عند تحميل الصفحة.
   *
   * صوت في الثانية الأولى مفاجأة مزعجة؛ وعند فتح الظرف يصير جزءاً من اللحظة.
   * وتبقى سياسة المتصفح فوق هذا كله: لا صوت قبل تفاعل حقيقي — وفتح الظرف تفاعل.
   */
  musicOnFirstOpen: true,
} as const;

/* =========================================================================
   Web Push
   ========================================================================= */

/**
 * المفتاح العام يُشحن مع الصفحة عمداً؛ ليس سرّاً ولا يرسل شيئاً وحده. السرّ هو
 * المفتاح الخاص، وهو في أسرار المستودع ولا يوجد في أي ملف هنا.
 *
 * `endpoint` هو الـWorker الذي يستقبل الاشتراك ويخزّنه. لو فُرِّغ، لا يُطلب
 * اشتراك ويبقى الإشعار محلياً — ونصّ الواجهة يتبع هذه القدرة لا العكس.
 */
export const PUSH = {
  publicKey: 'BEN4ah0KM0YmbXjwz6EMi9gBeolZiTYTL2d6MrstTmXRYDlHid-A9poYACkbeBByBMuQR8cSutjhcDuMNjR3sIY',
  endpoint: 'https://raghd-push.znad.workers.dev',
} as const;

/* =========================================================================
   الأغنية
   ========================================================================= */

export const SONG = {
  track: 'مش عم تروحي من بالي',
  artist: 'مروان خوري',
  /** معرّف يوتيوب للاحتياط إن حُذف الملف المحلي — فارغ = لا احتياط. */
  videoId: '',
} as const;
