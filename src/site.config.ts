/**
 * site.config.ts — الحقائق التي يقوم عليها الموقع
 *
 * كل ما قد يتغيّر يوماً موجود هنا، لا داخل المكوّنات. الموقع كله يقرأ من هذا
 * الملف: العنوان، العدّادات، موعد رسالة اللطافة، المفاجأة، وبيانات الأغنية.
 */

/** لمن هذا الموقع. */
export const HER = 'رغد';

/** ومن مين. */
export const FROM = 'إبراهيم';

/**
 * التواريخ مكتوبة بساعة الحائط في دمشق، لا بـUTC.
 *
 * الصيغة `YYYY-MM-DD` أو `YYYY-MM-DDTHH:MM` إن كانت الساعة معروفة. قيمة `null`
 * تُخفي العدّاد المعنيّ بدل أن تعرض أرقاماً مخترعة.
 */

/** يوم الصدفة — 25 كانون الثاني 2026. العدّاد الكبير يعدّ منه. */
export const COINCIDENCE: string | null = '2026-01-25';

/** يوم ما بلّشنا نحكي — 21 آذار 2026. عدّاد ثانٍ. */
export const FIRST_TALK: string | null = '2026-03-21';

/** موعد رسالة اللطافة اليومية — 11:00 صباحاً بتوقيت دمشق. */
export const SWEET_HOUR = 11;
export const SWEET_MINUTE = 0;

/**
 * المفاجأة 🎁
 *
 * لا يوجد عدّاد ولا موعد هنا **عن قصد**: الموقع نفسه لا يعرف متى. أي تاريخ
 * نضعه هنا يصل إلى متصفّحها ويُقرأ من أدوات المطوّر، فينكشف السرّ قبل وقته.
 *
 * كيف يعمل الكشف:
 *   1. الآن — `revealed: false` و`message: ''`. الموقع يعرض الظرف مقفولاً،
 *      ولا يحمل نص المفاجأة إطلاقاً، فلا شيء يمكن تسريبه.
 *   2. يوم المفاجأة — اكتب `message` واجعل `revealed: true` وانشر. أول مرة
 *      تفتح فيها رغد الصفحة بعدها، تنكشف الرسالة مع القلوب والفراشات.
 *
 * أي أن **لحظة النشر هي لحظة الكشف**. هذا هو الشيء الوحيد الذي يبقي السرّ سرّاً
 * في موقع ثابت بلا سيرفر.
 */
export const SURPRISE = {
  /** اقلبها إلى true يوم المفاجأة — وليس قبله. */
  revealed: false,

  /** نص المفاجأة. اتركه فارغاً حتى يوم الكشف؛ ما يُكتب هنا يُشحن مع الصفحة. */
  message: '',

  /** سطر صغير تحت الرسالة المكشوفة — اختياري. */
  footnote: '',
} as const;

/**
 * Web Push — الإشعار الذي يصل والمتصفّح مغلق تماماً
 *
 * المفتاح العام يُشحن مع الصفحة عمداً؛ هو ليس سرّاً، ولا يصلح لإرسال أي شيء
 * وحده. السرّ هو المفتاح الخاص، وهو محفوظ في أسرار المستودع (`VAPID_PRIVATE_KEY`)
 * ولا يوجد في أي ملف هنا.
 *
 * `endpoint` هو عنوان الـWorker الذي يستقبل اشتراك رغد ويخزّنه. ما دام فارغاً،
 * لا يُطلب اشتراك Push إطلاقاً ويبقى الإشعار اليومي محلياً فقط — فيعمل الموقع
 * كما هو بلا أعطال حتى يُنشر الـWorker.
 */
export const PUSH = {
  publicKey: 'BEN4ah0KM0YmbXjwz6EMi9gBeolZiTYTL2d6MrstTmXRYDlHid-A9poYACkbeBByBMuQR8cSutjhcDuMNjR3sIY',
  endpoint: '',
} as const;

/** الأغنية التي تشتغل بالخلفية. */
export const SONG = {
  track: 'عيني اليمين',
  artist: 'حمد العامري',
  /** معرّف يوتيوب للاحتياط إن حُذف الملف المحلي — فارغ = لا احتياط. */
  videoId: '',
} as const;
